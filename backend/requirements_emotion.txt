# Requirements for the MyFocusFriend emotion analysis server
# Run: pip install -r requirements_emotion.txt

fastapi==0.111.0
uvicorn==0.30.1          # the server that runs FastAPI
deepface==0.0.93         # emotion detection AI
opencv-python==4.10.0.84 # camera + image processing
numpy==1.26.4
tf-keras                 # required by deepface under the hood
